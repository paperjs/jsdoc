/**
 * @constructor
 * @param person The person.
 * @config {string} name The person's name.
 * @config {integer} [age] The person's age (optional).
 * @param connection
 * @config [protocol=http]
 * @config connection.URL The URL to use.
 */
function Contact(person, connection) {
}

